#this rio package will make image file conversion possible(through itself or tell you what package to use)
install.packages("rio")

complete_data <- import('data/complete_data.sas7bdat')

# Downloaded Rworkflow in the same name folder